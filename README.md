# Basic "To-do" app

This project is simply for learning purposes containing a client (simple_login) and a server (backend).

Frontend: React
To run this use:
```
cd simple_login/simple_login
npm run dev
```

Backend: Expressjs
Database: Postgresql
To run this use:
```
cd simple_login/backend
docker compose up --build
```
